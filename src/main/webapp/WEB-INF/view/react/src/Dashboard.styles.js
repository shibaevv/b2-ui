import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(() => ({
  grid: {
    background: 'white',
    height: '400px',
    width: '100%',
  },
  content: {
    display: 'flex',
  },
}))
